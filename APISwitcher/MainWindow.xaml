<Window x:Class="APISwitcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:APISwitcher"
        mc:Ignorable="d"
        Title="Claude Code 账号切换工具" Height="500" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Claude Code 账号配置切换"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="选择一个配置文件来切换 Claude Code CLI 的账号设置"
                       FontSize="12"
                       Foreground="Gray"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- 配置列表 -->
        <Border Grid.Row="1"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="5"
                Background="White">
            <ListBox ItemsSource="{Binding Profiles}"
                     SelectedItem="{Binding SelectedProfile}"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="15,10"
                                Background="{Binding IsActive, Converter={StaticResource ActiveBackgroundConverter}}"
                                BorderBrush="{Binding IsActive, Converter={StaticResource ActiveBorderConverter}}"
                                BorderThickness="2"
                                CornerRadius="5"
                                Margin="5"
                                Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                            Command="{Binding DataContext.SwitchProfileCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}"
                                             FontSize="16"
                                             FontWeight="Bold"
                                             Foreground="#333333"/>
                                    <TextBlock Text="{Binding Settings.ApiUrl, StringFormat='API URL: {0}'}"
                                             FontSize="12"
                                             Foreground="#666666"
                                             Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding Settings.Model, StringFormat='Model: {0}'}"
                                             FontSize="12"
                                             Foreground="#666666"
                                             Margin="0,2,0,0"/>
                                </StackPanel>

                                <Border Grid.Column="1"
                                        Background="#4CAF50"
                                        CornerRadius="3"
                                        Padding="8,4"
                                        Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock Text="当前使用"
                                             Foreground="White"
                                             FontSize="12"
                                             FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button Content="刷新配置"
                    Command="{Binding RefreshCommand}"
                    Width="120"
                    Height="35"
                    FontSize="14"
                    Margin="5"
                    Style="{StaticResource ModernButtonStyle}"/>
        </StackPanel>

        <!-- 状态栏 -->
        <Border Grid.Row="3"
                Background="#F5F5F5"
                Padding="10"
                CornerRadius="5"
                Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ProgressBar Grid.Column="0"
                           Width="20"
                           Height="20"
                           IsIndeterminate="True"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                           Margin="0,0,10,0"/>

                <TextBlock Grid.Column="1"
                         Text="{Binding StatusMessage}"
                         FontSize="12"
                         VerticalAlignment="Center"
                         Foreground="#666666"/>
            </Grid>
        </Border>
    </Grid>
</Window>
